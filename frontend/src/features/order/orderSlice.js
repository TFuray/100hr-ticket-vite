// import axios from 'axios'

// const API_URL = '/api/orders/'

// // Create new Orders
// const createOrders = async (ordersData, token) => {
//   const config = {
//     headers: {
//       Authorization: `Bearer ${token}`
//     }
//   }

//   const response = await axios.post(API_URL, ordersData, config)

//   return response.data
// }

// // Get userorders 
// const getOrders = async token => {
//   const config = {
//     headers: {
//       Authorization: `Bearer ${token}`
//     }
//   }

//   const response = await axios.get(API_URL, config)

//   return response.data
// }

// // Delete user orders
// const deleteOrders = async (ordersId, token) => {
//   const config = {
//     headers: {
//       Authorization: `Bearer ${token}`
//     }
//   }

//   const response = await axios.delete(API_URL + ordersId, config)

//   return response.data
// }
